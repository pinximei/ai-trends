# 复制为同目录下的 product_hunt.credentials 后填写（该文件已 gitignore，不会提交）
#
# 方式 A — OAuth（与后台「API Key + APP Secret」一致，推荐）
PRODUCT_HUNT_API_KEY=
PRODUCT_HUNT_APP_SECRET=
#
# 方式 B — 仅 Access Token 直连（与后台勾选「仅使用 Access Token」一致）
# 填写下一行时，可留空上面的 API_KEY / APP_SECRET
# PRODUCT_HUNT_ACCESS_TOKEN=
