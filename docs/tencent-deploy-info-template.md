# AISoul 腾讯云部署信息填写模板

> 填完本文件后发给我，我会按此信息直接部署并回传访问地址与验收结果。

## 1) 服务器信息（必填）

- 服务器公网 IP：（示例 `203.0.113.10`，请填你的公网 IP）
- SSH 端口（默认 22）：22
- 登录用户（`root` 或 sudo 用户）：ubuntu
- 登录方式（密码 / 密钥）：密钥 / 密码（二选一）
- SSH 密码（如使用密码登录）：（勿写入仓库；仅私下发给运维）
- 服务器系统版本（如 Ubuntu 22.04）：Ubuntu 22.04 LTS

## 2) 域名与访问方案（必填）

- 主域名（如 `aisoul.com`）：
- 是否已备案（大陆机房必填）：是 / 否
- DNS 控制权是否在你手里：是 / 否
- 当前解析平台（腾讯云 DNSPod / 阿里云 / Cloudflare / 其他）：

### 访问方式（二选一，勾选一个）

- [ ] 同域路径
  - 前台：`https://你的域名/`
  - 后台：`https://你的域名/admin`
  - API：`https://你的域名/api`

- [ ] 子域分离
  - 前台：`https://www.你的域名`
  - 后台：`https://admin.你的域名`
  - API：`https://api.你的域名`

## 3) 证书与 HTTPS（必填）

- 证书方案（腾讯云 SSL / Let's Encrypt）：
- 80/443 端口是否可放行（安全组 + 系统防火墙）：是 / 否

## 4) 数据库方案（必填）

### 方案 A：先快速上线（SQLite）

- [ ] 先用 SQLite（后续再迁移 PostgreSQL）

### 方案 B：直接生产（推荐，腾讯云 PostgreSQL）

- [ ] 直接用 PostgreSQL
- PostgreSQL 连接串（可脱敏）：
  - `postgresql+psycopg://user:pass@host:5432/dbname`
- 是否允许应用服务器访问数据库（白名单 / 安全组）：是 / 否

## 5) 项目信息（必填）

- Git 仓库地址：
- 部署分支（默认 `main`）：
- 服务器部署目录（默认 `/opt/aisoul`）：

## 6) 管理员初始化（必填）

- 管理员用户名（建议 `admin`）：
- 管理员初始密码（强密码）：

## 7) 运行策略（选填）

- 是否需要开机自启动：是 / 否
- 是否需要日志轮转：是 / 否
- 是否需要每日自动备份：是 / 否
- 备份目标（本机 / COS / 其他）：

## 8) 你希望我完成的交付结果（默认全做）

- [ ] 前台可访问
- [ ] 后台可访问并登录
- [ ] API 可访问
- [ ] HTTPS 生效
- [ ] systemd 自启动配置完成
- [ ] Nginx 配置完成
- [ ] 验收截图/结果说明

## 9) 安全授权确认（必填）

请你确认以下授权（勾选）：

- [ ] 我授权你使用以上信息进行部署操作
- [ ] 我知晓部署过程中可能会安装系统软件并修改 Nginx/systemd 配置
- [ ] 我知晓你不会把密钥文件提交到 git
- [ ] 部署完成后我会立即修改临时密码/密钥（如使用了临时凭据）

---

## 10) 填写后发送格式（复制这段回我）

请按以下格式回复我：

```text
[部署信息已填写]
文件：docs/tencent-deploy-info-template.md
可开始部署：是
```

